DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': 'django_summernote.db',
    }
}

ROOT_URLCONF = 'django_summernote.urls'

INSTALLED_APPS = (
    'django_summernote',
)
